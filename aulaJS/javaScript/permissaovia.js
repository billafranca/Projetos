var velocidade = prompt("Digite a velocidade do automóvel (km/h): ");

if (velocidade > 90) {
  alert("REGISTRO DE INFRAÇÃO POR EXCESSO DE VELOCIDADE AO PERMITIDO NA VIA!");
} else {
  alert("VELOCIDADE DO AUTOMÓVEL ESTÁ ABAIXO DO LIMITE DA VIA!");
}